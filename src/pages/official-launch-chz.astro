---
import Analytics from "@vercel/analytics/astro";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>$CHZ Official Launchpad</title>
    <style>
      :root {
        color-scheme: dark;
        --bg-dark: #050309;
        --brand-fire: #ff7930;
        --brand-ember: #ffd166;
        --text-primary: #f7f7ff;
        --text-muted: rgba(247, 247, 255, 0.7);
        font-family: "Segoe UI", system-ui, sans-serif;
      }

      body {
        margin: 0;
        background: var(--bg-dark);
        color: var(--text-primary);
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-image: radial-gradient(circle at top, rgba(255, 111, 28, 0.15), transparent 70%);
      }

      .container {
        max-width: 600px;
        width: 95%;
        background: rgba(17, 10, 26, 0.8);
        padding: 3rem;
        border-radius: 32px;
        border: 1px solid rgba(255, 121, 48, 0.3);
        backdrop-filter: blur(20px);
        box-shadow: 0 20px 50px rgba(0,0,0,0.5);
      }

      h1 {
        text-align: center;
        color: var(--brand-fire);
        margin-bottom: 0.5rem;
        letter-spacing: 2px;
      }

      .admin-badge {
        display: block;
        text-align: center;
        font-size: 0.7rem;
        background: #ff4c20;
        color: #000;
        padding: 2px 8px;
        border-radius: 4px;
        width: fit-content;
        margin: 0 auto 1.5rem;
        font-weight: 800;
      }

      .step-card {
        background: rgba(255,255,255,0.03);
        border: 1px solid rgba(255,255,255,0.1);
        padding: 1.5rem;
        border-radius: 16px;
        margin-bottom: 1.5rem;
      }

      .step-number {
        background: var(--brand-fire);
        color: #000;
        width: 24px;
        height: 24px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        font-weight: 800;
        margin-right: 10px;
      }

      h3 {
        margin: 0 0 1rem;
        color: var(--brand-ember);
      }

      p {
        color: var(--text-muted);
        font-size: 0.9rem;
        line-height: 1.4;
      }

      .btn {
        display: block;
        width: 100%;
        padding: 1rem;
        border-radius: 12px;
        border: none;
        font-weight: 800;
        text-align: center;
        text-decoration: none;
        cursor: pointer;
        margin-top: 1rem;
        text-transform: uppercase;
      }

      .btn-primary {
        background: var(--brand-fire);
        color: #000;
      }

      .btn-secondary {
        background: rgba(255,255,255,0.1);
        color: #fff;
        border: 1px solid rgba(255,255,255,0.2);
      }

      code {
        background: #000;
        padding: 4px 8px;
        border-radius: 4px;
        color: #fff;
        font-family: monospace;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <span class="admin-badge">RELIABLE BRANDING GUIDE</span>
      <h1>FINALIZE $CHZ</h1>
      
      <div class="step-card">
        <h3><span class="step-number">1</span>Verify Mint Address</h3>
        <p>Your official token has already been created. This is the address:</p>
        <code style="display:block; margin: 10px 0; word-break: break-all;">7pQ6NNrUN73n5xEFqczgG3Yox6ZzTzW5g7aYgC1EhkHe</code>
      </div>

      <div class="step-card">
        <h3><span class="step-number">2</span>Manual Branding (Safe Way)</h3>
        <p>Since the automated browser tool is hitting security blocks in your Phantom, use the 100% official way to upload your logo and name.</p>
        
        <p style="margin-top: 1rem;"><strong>Recommended Tool:</strong> <a href="https://v1.slr.tools/token-branding" target="_blank" style="color:var(--brand-fire)">SLR Token Branding</a> (Verified for Solana Mainnet).</p>
        
        <ul style="color: var(--text-muted); font-size: 0.85rem; padding-left: 20px; margin-top: 10px;">
          <li>Connect your wallet: <code>9Vsan6...5wP</code></li>
          <li>Enter Mint: <code>7pQ6NNrUN73n5xEFqczgG3Yox6ZzTzW5g7aYgC1EhkHe</code></li>
          <li>Name: <code>Charizard Token</code> | Symbol: <code>CHZ</code></li>
          <li>Logo: Upload your <code>logo.png</code></li>
        </ul>
      </div>

      <div class="step-card">
        <h3><span class="step-number">3</span>Alternative: CoinBrain</h3>
        <p>If you plan to list on exchanges soon, the fastest way to get your logo appearing in wallets is to "Claim your token" on <a href="https://coinbrain.com/coins/solana-7pQ6NNrUN73n5xEFqczgG3Yox6ZzTzW5g7aYgC1EhkHe" target="_blank" style="color:var(--brand-fire)">CoinBrain</a> or <strong>DexTools</strong>.</p>
      </div>

      <a href="/" class="btn btn-secondary">← Back to Website</a>
    </div>
  </body>
</html>
